# Dockerfile for FastAPI backend
FROM python:3.11-slim

WORKDIR /app

COPY backend/ ./backend/
COPY requirements.txt ./

RUN pip install --no-cache-dir -r requirements.txt

EXPOSE 8001

CMD ["uvicorn", "backend.backend_main:app", "--host", "0.0.0.0", "--port", "8001"]
